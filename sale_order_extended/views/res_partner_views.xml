<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Partner form -->
    <record id="view_res_partner_add_repeat_count_tree" model="ir.ui.view">
        <field name="name">res.partner.repeat.count.tree</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <field name="display_name" position="after">
                <field name="repeat_count_eg"/>
                <field name="total_ordered_amt_eg"/>
            </field>
        </field>
    </record>

</odoo>
