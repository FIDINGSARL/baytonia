<odoo>
    <record id="sale_order_form_inherited_delivery_note" model="ir.ui.view">
        <field name="name">sale.order.form.inherited.delivery.note</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="note" position="after">
                <field name="delivery_note_eg" class="oe_inline"
                       placeholder="Delivery note Need to be entered here ..."/>
            </field>
        </field>
    </record>
</odoo>