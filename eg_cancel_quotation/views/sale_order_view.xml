<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="action_cancel_quotation_order" model="ir.actions.server">
        <field name="name">❌Quotation Cancel❌</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="binding_model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">records.action_cancel_quotation_order()</field>
    </record>

    <record id="action_confirm_quotation_order" model="ir.actions.server">
        <field name="name">✔️Confirm Sale Order✔️</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="binding_model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">records.action_confirm_sale_order()</field>
    </record>

</odoo>